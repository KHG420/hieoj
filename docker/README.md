# 当前 Docker 配置

从**仓库根目录**执行 `docker compose up -d --build`，不要在本目录另起一套服务。

启动、密码读取、语言支持和数据卷说明见[根 README](../README.md)。
生产数据导入、备份及回滚见[迁移说明](MIGRATION.md)。
此前依赖 `/opt/hnieoj-docker`、宿主源码和手工沙箱镜像的说明已归档到 `docs/legacy/`。
