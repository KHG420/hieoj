# 学院班级同步生产部署记录

日期：2026-09-14，Asia/Shanghai。状态：部署完成并通过线上验证。

## 版本与部署目标

- 仓库：`git@github.com:KHG420/hieoj.git`，分支 `main`。
- 应用提交：`d2f04ee7e71194d27f6156429ef6e95462171059`。
- 部署前提交：`0e5557dbe6488e6c6912fc28dfcdda27a5da1eaf`。
- 本次 SSH 地址：`172.31.0.96`；使用原 `10.95.222.28` 的已知主机密钥校验通过。
- 活动目录：`/opt/hnieoj`；Compose 项目：`hnieoj-unified`；入口：根 `compose.yaml`。
- 正式网站：<https://www.hnieacm.com>。
- 部署方式：服务器 Git fast-forward pull 后构建并重建 Web；不经过 Dokploy。
- 本记录为后续文档提交，不改变上述已部署应用代码。

## 范围与运行配置

仅重建 Web。数据库、缓存和判题容器保持原部署；既有卷、环境变量、域名、端口和公网代理未修改。Web 继续监听 `127.0.0.1:80`，数据库和缓存仍仅在原 Compose 网络中使用。数据库凭据继续来自原 credentials 卷，没有新增、替换或删除配置键，也没有部署定时学院同步任务。

Web 新镜像：`sha256:ea3dd80e527ef42e46a0bc4fea09c5158842151800b3073e23ba892dbe3bef62`。
现有 Ubuntu 22.04 / PHP 8.1 镜像依赖层复用构建缓存，无新增生产依赖或镜像源变更。

## 备份与数据库迁移

上线前执行项目 `docker/operations/backup.sh`，备份位置：
`/var/backups/hnieoj-unified/20260914-232422/`。

数据库、题目与上传文件、凭据、Nginx 状态四份压缩备份全部通过 SHA-256 与 gzip 完整性检查。备份保留在生产服务器受限目录，不入 Git。

迁移先 dry-run，再运行 `web/cli/academic_directory_migrate.php --apply`，共执行 14 项操作：

- 为两张目录表增加来源标识和必要索引。
- 将班级编号扩为 VARCHAR(64)，将 schoolList 转为 InnoDB，支持同步事务回滚。
- 补齐体育科学与工程学院、智能科学与工程学院、医学工程技术学院。
- 将已有 id=9 的学院名称更新为智慧建造与能源工程学院。

迁移完成后再次 dry-run，actions 为空，两表均为 InnoDB、来源字段存在、班级编号宽度为 64。
学院从 14 个增至 17 个；原 1,563 条班级记录完整保留。
12,120 名用户的数量和表校验值不变，原班级全部业务字段的 SHA-256 不变。
服务器备份目录的 `academic-before.json` 和 `academic-after.json` 经 cmp 完全一致。

本次未向生产导入本地教务快照。最新学院和班级应由管理员从后台填写本次认证信息、采集并预览后确认同步。此前真实教务 22 个学院、4,141 个班级的全流程验证，以及缺失学院/班级恢复测试，均在本地完成。

## 线上验证

- Web、db、judge 均 running / healthy，cache running；三个受检查容器重启次数均为 0。
- Nginx 配置检查与 Web/数据库健康检查通过。
- 公网首页、登录、注册、排名、题库、状态和 getClass 返回 HTTP 200。
- getClass 无筛选参数仍返回空数组；注册页包含迁移补齐的学院。
- 未登录访问 `/admin/academic_directory.php` 返回 HTTP 403。
- 在生产容器中使用一次性验证会话进行真实 HTTP 请求：管理员 GET 返回 200，初始验证码表单正确；POST 获取验证码返回 200，实际教务验证码图片和临时凭据表单均正常。
- 临时管理员验证会话全部清理，未创建或修改数据库用户及权限，没有输入或保存教务账号密码。
- Web 最近运行日志未发现 PHP Fatal/Warning、SQLSTATE、数据库认证或连接拒绝等错误。
- 临时迁移容器使用 --rm，结束后无残留；生产 Git 工作区干净。

验证过程中首次管理员页面检查误要求在获取验证码之前显示第二步采集表单；按实际分步流程修正验收脚本后，GET 和验证码 POST 均通过。产品代码无需修改。

## 回退依据

部署前 Web 镜像已保留为 `hnieoj-unified-web:pre-academic-d2f04ee`，镜像 ID：
`sha256:0ea3f3c24181cc1dbfe06d9c69b279670b0d93cb31bbdf380baec4968bf41a4d`。

回退代码时保留现有业务卷和数据。此次新增字段/索引及编号扩容不能通过代码回退自动撤销；DDL 已提交。如需回退数据库结构或数据，应基于上述备份制定单独方案，不能直接覆盖上线后新增的业务数据，也不能删除生产卷。
