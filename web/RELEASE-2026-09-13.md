# 2026-09-13 桌面前端发布记录（事后补录）

本记录在用户授权后补齐；Git 提交时间是补录时间，不代表最初开发或部署时间。此前功能与本次桌面优化分开提交，未重写原有历史。

## 代码边界

- `566d135`：此前知识地图功能，含标签映射、掌握度、关系图、列表、管理入口与逻辑测试。重叠文件使用本地桌面改造前备份恢复到 Git 暂存区，没有倒退运行中的工作树。
- `8229b6228d2b8f0aeb4f0c5d8b8ad955a2bb4a7b`：本次上线的 18 个桌面改造文件，与生产部署文件逐字节一致。混合换行及题库模板两处原有尾随空格保留，不为美化 diff 改变发布内容。
- 本记录、SHA-256 清单及前端回归测试作为后续文档提交；生产 Web 目录不要求包含这些补录文件。

## 发布与回退

- 线上入口：https://www.hnieacm.com/ 。部署至内网 Web 服务，经原有 FRP 对外提供服务。
- 发布前 17 个原有文件与改造前备份哈希一致；新增 desktop.css。发布后 18 个文件与本地哈希一致。
- `RELEASE-2026-09-13.sha256` 是本次部署文件清单，可在 Web 根目录执行 `sha256sum -c RELEASE-2026-09-13.sha256`；macOS 使用 `shasum -a 256 -c RELEASE-2026-09-13.sha256`。
- 服务器上线前备份：`/var/tmp/hnieoj-desktop-20260913.OGla9S/before.tar.gz`。回退应定向恢复其中 17 个原有文件；新增 CSS 不再被旧模板引用，无需删除。恢复后使相关页面缓存失效。
- 未修改数据库、DNS、FRP、判题机或依赖配置，未重启服务。已让旧版整页缓存过期，首页复查通过。
- 该标签描述 Web 代码及上述部署文件，不是全系统镜像或数据库快照。知识图谱已有数据库迁移位于外层开发目录 `runtime-root/opt/hnieoj-docker/db/`，不在本 Web Git 仓库内，不能仅凭此仓库在空数据库上还原完整服务。

## 验证

- 服务器端 16 个发布 PHP 文件语法检查通过。
- 内网和公网只读集成回归通过：搜索 327 条、7 页、末页 27 条，跨页无重复；空结果、越界页、讨论区、个人页和登录入口通过。
- 公网 10 个前台页面 HTTP 200，未发现 pageerror；首页缓存失效后新版 CSS 和布局正确。
- 公网点击“主页 → 问题 → 知识地图”始终只有一个浏览器标签页，没有复现另开标签页。
- 本地 `tests/knowledge_graph_test.php` 通过；生产未安装此测试文件，未宣称在生产执行了该单元测试。
- 补录时再次运行本地知识图谱测试及前端回归，均通过。
- 既有后台 Popper 缺失未处理；不包含真实注册、提交判题及后台保存/删除验收。

本轮仅补齐本地 Git 记录和发布标签，没有推送远程仓库或更改生产服务器 Git 历史。提交者 Codex 的地址仅作为本地自动化身份，不改变用户的 Git 身份配置。
